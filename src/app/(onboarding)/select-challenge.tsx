import { Screen } from "@/components/ui/Screen";
import { Text } from "@/components/ui/Text";

export default function SelectChallengeRoute() {
  return (
    <Screen subtitle="Onboarding scaffold" title="Select challenge">
      <Text tone="muted" variant="body">
        Challenge selection screen placeholder. Real seeded cards will be connected in the onboarding phase.
      </Text>
    </Screen>
  );
}
