import { Screen } from "@/components/ui/Screen";
import { Text } from "@/components/ui/Text";

export default function HomeRoute() {
  return (
    <Screen subtitle="Tabs scaffold" title="Home">
      <Text tone="muted" variant="body">
        Home tab placeholder. The temporary Hello World has been moved into the required routed structure.
      </Text>
    </Screen>
  );
}
